[module/date]
type = internal/date
interval = 1

date = %Y-%m-%d %H:%M:%S
date-alt = %H:%M

format = "<label> "
format-background = "${colors.green}"

format-prefix = "%{T3}%{T-} "
format-prefix-foreground = "${colors.white}"

label = %date%
label-foreground = ${colors.white}
